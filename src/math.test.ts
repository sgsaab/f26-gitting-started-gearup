import { add2, sub2 } from './math';

describe("setup", () => {
  test("jest is configured correctly", () => {
    expect(true).toBe(true);
  });
});

// TODO: Have the agent write some tests for add2 and sub2